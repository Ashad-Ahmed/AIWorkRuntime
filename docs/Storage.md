# Storage

Storage is accessed through protocols in `awr/storage/base.py`. SQLite is the first implementation; PostgreSQL, Redis, object storage, and memory stores can be added behind the same runtime interfaces.
