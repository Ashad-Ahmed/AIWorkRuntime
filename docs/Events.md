# Events

Events are immutable records containing event id, timestamp, work id, type, payload, producer-compatible metadata, and version-ready structure. They are persisted in `runtime_events` and can be replayed into a state projection.
