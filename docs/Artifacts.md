# Artifacts

Artifacts are first-class metadata records with id, work id, type, MIME type, path, timestamp, size, producer, version, and metadata. The initial store writes text artifacts locally while preserving an interface for future binary storage.
