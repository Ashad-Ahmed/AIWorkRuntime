# Supplier Profiling Demo

```bash
awr --db supplier.sqlite demo supplier-research
awr --db supplier.sqlite timeline
awr --db supplier.sqlite graph --mermaid
awr --db supplier.sqlite metrics
```

Shows parent/child work, dependencies, execution adapter assignment, artifacts, metrics, and replayable events.
